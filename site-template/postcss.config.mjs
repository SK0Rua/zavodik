/** Tailwind CSS v4 uses a PostCSS plugin; there is no tailwind.config.js.
 *  All theme/keyframe configuration lives in app/globals.css under @theme. */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
