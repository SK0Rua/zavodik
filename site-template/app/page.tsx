// PLACEHOLDER: the builder agent replaces this entirely with the personalized
// site (sections per the content brief, design per the chosen art direction).
export default function Home() {
  return (
    <main style={{ padding: 40, fontFamily: 'system-ui' }}>
      <h1>Demo site placeholder</h1>
      <p>If you see this, the builder agent has not customized the template yet.</p>
    </main>
  );
}
