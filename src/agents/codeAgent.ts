/**
 * Back-compat facade for the workspace (code) agent.
 * Implementation: `runtime.ts` -> Claude Code / Codex adapters.
 */
export { runCodeAgent, z } from './runtime.js';
export type { CodeAgentOptions } from './types.js';
